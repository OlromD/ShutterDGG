import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container : {
    flex: 1,
    backgroundColor: '#eee',
    // flexDirection: 'row'
  },
  caption : {
    fontSize: 25,
    margin: 10,
    textAlign : 'center'
  }
});
